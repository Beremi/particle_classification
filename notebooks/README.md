# Notebooks

`demo_data.ipynb` is the original sparse-matrix orientation demo. Run it with
the repository root as the working directory; it reads
`data/matrix_dump_0001.txt` and writes generated media below `outputs/`.

The maintained T3PA, DBSCAN, and autoencoder workflows are command-line based
and documented in [`../docs`](../docs/README.md).
